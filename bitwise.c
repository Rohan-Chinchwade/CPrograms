#include<stdio.h>
int main()
{
     int long a,b=30;
     long long c;
     short z;
    // printf("Binary &:%d",(a&b));
    // printf("\nBinary OR:%d",(a|b));
    // printf("\nBinary XOR:%d",(a^b));
    // printf("\n1's compliment:%d",~a);
    // printf("\nLeft Shift:%d",a<<1);
    // printf("\nRight Shift:%d",a>>1);
    printf("Sizeof a:%d",sizeof(a));
    printf("\nsizeof of c:%d",sizeof(c));
    printf("\nsize of short z:%d",sizeof(z));


}